server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event_db
    username: event_user
    password: password
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
